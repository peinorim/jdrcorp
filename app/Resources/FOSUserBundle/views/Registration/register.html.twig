{% extends "::layout_elric.html.twig" %}
{% block title %}
    JdrCorp - Inscription
{% endblock %}

{% block robots %}
    <meta name="robots" content="index, nofollow">
{% endblock %}

{% block fos_user_content %}
    {% trans_default_domain 'FOSUserBundle' %}
    <div class="container">
        <div class="page-header">
            <h1>Inscription</h1>
        </div>
        <div class="well">
            <div class="page-header">
                <p><b>Notez que l'inscription est tout à fait facultative pour générer vos fiches.</b></p>
                <p>Une fois inscrit vous pourrez cependant profiter d'une sauvegarde automatique de vos fiches ainsi que
                    la possibilité de les visualiser.</p>
            </div>

            <form class="form-horizontal" id="fos_user_registration_register"
                  action="{{ path('fos_user_registration_register') }}"
                  method="POST">
                <div id="fos_user_registration_form">
                    <div class="form-group">
                        <label for="fos_user_registration_form_email" class="col-sm-2 control-label">Email</label>
                        <div class="col-sm-10">
                            {{ form_widget(form.email, { 'attr': {'class': 'form-control', 'placeholder': 'Email' } }) }}
                            {{ form_errors(form.email) }}
                        </div>
                    </div>
                    {{ form_widget(form.username, { 'attr': {'class': 'form-control hidden', 'value': 'username'} }) }}
                    {{ form_errors(form.username) }}

                    <div class="form-group">
                        <label for="fos_user_registration_form_plainPassword_first" class="col-sm-2 control-label">Mot
                            de passe : </label>
                        <div class="col-sm-10">
                            {{ form_widget(form.plainPassword.first, { 'attr': {'class': 'form-control', 'placeholder': 'Mot de passe' } }) }}
                            {{ form_errors(form.plainPassword.first) }}
                        </div>
                    </div>
                    <div class="form-group">
                        <label for="fos_user_registration_form_plainPassword_second" class="col-sm-2 control-label">Confirmez
                            votre mot de passe : </label>
                        <div class="col-sm-10">
                            {{ form_widget(form.plainPassword.second, { 'attr': {'class': 'form-control', 'placeholder': 'Confirmez votre mot de passe' } }) }}
                            {{ form_errors(form.plainPassword.second) }}
                        </div>
                    </div>

                    {{ form_rest(form) }}
                    <div class="form-group">
                        <div class="col-sm-offset-2 col-sm-10">
                            <input class="btn btn-lg btn-primary" type="submit"
                                   value="{{ "S'inscrire"|trans }}">
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>
{% endblock fos_user_content %}
