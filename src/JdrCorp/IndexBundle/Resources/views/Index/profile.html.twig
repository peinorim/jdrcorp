{% extends "::layout.html.twig" %}
{% block title %}
JdrCorp - Elric - Mes fiches
{% endblock %}
{% block fos_user_content %}
{% if is_granted('ROLE_USER') and myfiches|length >0 %}
<div class="panel panel-primary">
    <div class="panel-heading">
        <h3 class="panel-title">Vous avez créé {{myfiches|length}} fiches pour Elric</h3>
    </div>
    <div class="panel-body">

        <table class="table table-hover">
            <thead>
                <tr>
                    <th></th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                    {% for fiche in myfiches %}
                <tr>
                    <td class="col-md-2">Nom du personnage : {{fiche.getPerso.getNom}} -  Créé le : {{fiche.date | date('d/m/Y à H:i:s')}}</td>
                    <td class="col-md-1"><form action="{{path('Elric_review', {'id': fiche.getId, 'format': 'pdf' })}}" target="_blank" method="post"><button type="submit" class="btn btn-primary btn-sm">PDF</button></form></td>
                    <td class="col-md-1"><form action="{{path('Elric_review', {'id': fiche.getId, 'format': 'jpg' })}}" target="_blank" method="post"><button type="submit" class="btn btn-primary btn-sm">JPG</button></form></td>
                </tr>
                    {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endif %}
{% endblock fos_user_content %}
